{
  "name": "consumemate",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "chrome-extension"
  ],
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "backend": "npm run dev --workspace=backend",
    "extension": "npm run dev --workspace=chrome-extension",
    "heroku-prebuild": "rm -rf mobile ios swift",
    "heroku-postbuild": "npm run build --workspace=backend",
    "start": "npm run start --workspace=backend"
  }
}
